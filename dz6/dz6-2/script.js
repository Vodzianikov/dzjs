// 2) С помощью prompt запросить у пользователя его имя. Передайте в prompt строку “Как вас
// зовут?” и введенный пользователем результат сохраните в переменную userName. Какое бы
// значение пользователь не ввел, вам требуется отформатировать строку userName по следующим
// условиям:

let userName = prompt("Как вас зовут?");
function userNameFunc (){
    let userNameTLC = userName.toLowerCase()
    function userNameTr (name){
        let userNameTrc = userName.trim()
        let youNAme = 
    console.log (youNAme)
    console.log(`${name}`  `${userNameTrc}`)
    }
}
    userNameTr("Вас зовут")

